# How To Slim

1. download bootstrap-4.0.0 sources
2. `npm install`
3. copy `bootstrap.scss` and `_utilities.scss` into `scss` folder (overwrite original files)
4. `npm run css`
	- you can remove `css-lint*` in `package.json` for reducing build cost.
5. get slim file `dist/css/bootstrap.min.css`
